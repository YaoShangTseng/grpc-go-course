syntax = "proto3";

package sum;
option go_package = "sumpb";

message SumRequest {
  int32 first_num = 1;
  int32 second_num = 2;
}

message SumResponse { int32 sum = 1; }

service SumService {
  // Unary
  rpc Sum(SumRequest) returns (SumResponse) {};
};
